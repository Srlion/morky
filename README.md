# morky

A simple, rootless, self-hosted app platform.

## Install

```bash
curl -fsSL https://github.com/srlion/morky/raw/main/install.sh | bash
```

Requires a user with sudo access. The script will prompt for your password if needed.
